[project]
name = "roxy"
version = "0.1.0"
description = "Roxy: modular extraction of protein sequence descriptors"
readme = "README.md"
requires-python = ">=3.9"
license = "GPL-3.0-only"

authors = [
  { name = "Kren AI Lab", email = "krenai@umag.cl" },
]

keywords = [
  "bioinformatics",
  "protein-sequences",
  "protein-descriptors",
  "sequence-analysis",
  "descriptor-extraction",
  "aaindex",
]

classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
]

dependencies = [
  # Definitely needed by the active sequence-focused core and CLI.
  "pandas>=1.5",
  "requests>=2.28",
  "typer>=0.9",

  # Retained conservatively for near-term review.
  "numpy>=1.23",
  "scikit-learn>=1.2",
  "matplotlib>=3.6",
  "joblib>=1.2",
]

[project.optional-dependencies]
# Optional visualization extras.
viz = [
  "plotly>=5.0",
]

# Optional dimensionality-reduction extras.
umap = [
  "umap-learn>=0.5",
]

# Developer tools: tests, linting, formatting, notebooks
dev = [
  "pytest>=7.0",
  "pytest-cov>=4.0",
  "mypy>=1.0",
  "ruff>=0.4",
  "black>=24.0",
  "ipykernel",
  "nbformat",
  "nbclient",
]

# Convenience extra to install "everything"
all = [
  "plotly>=5.0",
  "umap-learn>=0.5",
  "pytest>=7.0",
  "pytest-cov>=4.0",
  "mypy>=1.0",
  "ruff>=0.4",
  "black>=24.0",
  "ipykernel",
  "nbformat",
  "nbclient",
]

[project.scripts]
roxy = "roxy.cli:app"

[project.urls]
homepage = "https://github.com/ProteinEngineering-PESB2/roxy_library"
repository = "https://github.com/ProteinEngineering-PESB2/roxy_library"
issues = "https://github.com/ProteinEngineering-PESB2/roxy_library/issues"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = { "" = "." }

[tool.setuptools.packages.find]
where = ["."]
include = ["roxy*"]


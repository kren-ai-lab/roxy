"""User-defined regex-driven sequence motif descriptors.

This module is the dedicated home for arbitrary regex pattern logic supplied
by the user. No concrete regex descriptor API is implemented in the current
codebase yet, so the module intentionally stays empty rather than adding
placeholder behavior.
"""

__all__: tuple[str, ...] = ()
